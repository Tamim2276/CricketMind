% PREVIEW ONLY -- not part of the paper.
% Shows both results subsections on two-column pages, roughly the size of a
% conference template, so tables and figures can be checked for fit.
% Compile from the paper/ folder:   tectonic preview_results.tex
\documentclass[10pt,twocolumn]{article}
\usepackage[margin=0.75in,columnsep=0.25in]{geometry}
\usepackage{booktabs,array,graphicx,amsmath}
\usepackage{fontspec}   % Times (TeX Gyre Termes), as most two-column templates use;
\setmainfont{texgyretermes}[Extension=.otf, UprightFont=*-regular, BoldFont=*-bold,
  ItalicFont=*-italic, BoldItalicFont=*-bolditalic]   % mathptmx does not work with Tectonic

\begin{document}

% Stand-in for the paper's architecture figure, so Fig.~\ref{fig:arch-v2}
% resolves to "1" here instead of printing "??".
\refstepcounter{figure}\label{fig:arch-v2}

\section{Results}
\input{results_execution_profile}
\input{results_routing}

\end{document}
